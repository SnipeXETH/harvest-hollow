{
  "name": "harvest-hollow",
  "version": "0.1.0",
  "description": "A cozy click-to-farm mobile game inspired by early FarmVille.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  }
}
